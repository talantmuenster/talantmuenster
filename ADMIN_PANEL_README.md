# Админ-панель для управления контентом

## Обзор

Эта админ-панель предоставляет полноценный интерфейс для управления контентом сайта с поддержкой трёх языков (русский, английский, немецкий) и интеграцией с Firebase.

## Функции

### 🎯 Основные возможности

- **Авторизация администраторов** через Firebase Authentication
- **Управление событиями** - создание, редактирование, удаление событий
- **Управление новостями** - публикация новостей с многоязычным контентом
- **Управление постами** - создание и редактирование постов блога
- **Многоязычность** - поддержка русского, английского и немецкого языков
- **Статус публикации** - возможность сохранять как черновики или публиковать

### 📝 Управление контентом

- Редактор с вкладками для каждого языка
- Превью содержимого на всех языках
- Автогенерация слагов для URL
- Поддержка изображений и обложек
- Валидация обязательных полей

## Установка и настройка

### 1. Настройка Firebase

#### Создание проекта Firebase

1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Authentication и Firestore Database

#### Настройка Authentication

1. В разделе Authentication включите Email/Password провайдера
2. Создайте учетную запись администратора

#### Настройка Firestore

1. Создайте базу данных Firestore
2. Настройте правила безопасности:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Только авторизованные пользователи могут читать/писать
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

#### Получение конфигурации

1. Перейдите в Project Settings > General
2. Скопируйте конфигурацию Web App
3. В разделе Service Accounts сгенерируйте новый приватный ключ

### 2. Настройка переменных окружения

Создайте файл `.env.local` на основе `.env.example`:

```bash
cp .env.example .env.local
```

Заполните следующие переменные:

```env
# Firebase Client Configuration
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Firebase Admin Configuration (Service Account)
FIREBASE_ADMIN_KEY={"type":"service_account",...}
```

### 3. Установка зависимостей

```bash
npm install
# или
yarn install
# или
pnpm install
```

### 4. Запуск проекта

```bash
npm run dev
# или
yarn dev
# или
pnpm dev
```

## Структура админ-панели

### Маршруты

- `/admin/login` - Страница авторизации
- `/admin` - Главная панель администратора
- `/admin/events` - Управление событиями
- `/admin/news` - Управление новостями
- `/admin/posts` - Управление постами

### Компоненты

- `AuthContext` - Контекст авторизации
- `LanguageEditor` - Редактор многоязычного контента
- `AdminLayout` - Защищённый макет админ-панели

### Сервисы

- `eventService` - CRUD операции для событий
- `newsService` - CRUD операции для новостей
- `postService` - CRUD операции для постов

## Использование

### 1. Авторизация

1. Перейдите на `/admin/login`
2. Введите email и пароль администратора
3. После успешной авторизации вы будете перенаправлены на главную панель

### 2. Управление событиями

- Создайте события с датой, временем и описанием
- Добавьте ссылку на регистрацию (опционально)
- Заполните контент на всех трёх языках

### 3. Управление новостями

- Создайте новости с заголовком, подзаголовком и контентом
- Добавьте изображения и обложки
- Выберите статус: черновик или опубликованная
- Автоматическая генерация URL на основе заголовка

### 4. Управление постами

- Создайте посты блога с заголовком и контентом
- Добавьте краткое описание для превью
- Управляйте статусом публикации

## Структура данных

### События (CalendarEvent)

```typescript
{
  id: string;
  title: LocalizedContent;
  description: LocalizedContent;
  date: string;
  startTime: string;
  endTime: string;
  registrationUrl?: string;
  createdAt: string;
  updatedAt: string;
}
```

### Новости (News)

```typescript
{
  id?: string;
  slug: LocalizedContent;
  title: LocalizedContent;
  subtitle: LocalizedContent;
  description: LocalizedContent;
  image: string;
  date: string;
  href: string;
  cover: string;
  content: LocalizedContent;
  published: boolean;
  createdAt: string;
  updatedAt: string;
}
```

### Посты (Post)

```typescript
{
  id?: string;
  title: LocalizedContent;
  content: LocalizedContent;
  excerpt: LocalizedContent;
  featuredImage?: string;
  published: boolean;
  createdAt: string;
  updatedAt: string;
}
```

### Многоязычный контент (LocalizedContent)

```typescript
{
  ru: string;
  en: string;
  de: string;
}
```

## Безопасность

- Все операции требуют авторизации
- Firebase правила ограничивают доступ только авторизованным пользователям
- Клиентские маршруты защищены с помощью AuthContext
- Серверные операции используют Firebase Admin SDK

## Поддерживаемые браузеры

- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

## Поддержка

Если у вас возникли проблемы с настройкой или использованием админ-панели, проверьте:

1. Правильность настройки переменных окружения
2. Конфигурацию Firebase
3. Правила Firestore
4. Настройки Authentication

## Лицензия

Этот проект создан для внутреннего использования.
