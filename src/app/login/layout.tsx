// Login page - rendered without admin layout

export const metadata = {
  title: 'Admin Login - Talant',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
